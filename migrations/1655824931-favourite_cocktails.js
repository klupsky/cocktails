exports.up = async (sql) => {
  await sql`
    CREATE TABLE favourite_cocktails (
      id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
      cocktail_id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    )
  `;
};

exports.down = async (sql) => {
  await sql`
    DROP TABLE favourite_cocktails
  `;
};
